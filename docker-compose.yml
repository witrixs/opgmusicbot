services:
  bot:
    image: opgmusicbot:latest
    build: .
    container_name: opgmusic
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - /etc/localtime:/etc/localtime:ro